<ul>
  <% @tasks.each do |task| %>
    <li> <%= task.completed ? "Done" : "to be done"%> <%=link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<<%= link_to  "create a new task", tasks_new_path %>>